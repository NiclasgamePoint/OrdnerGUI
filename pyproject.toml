[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ordnergui"
version = "0.3.0"
description = "PapaGUI - Kundenmanagement System"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools.package-data]
app = ["resources/*.txt", "resources/*.md"]
